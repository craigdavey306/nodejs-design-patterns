Demonstrates the differences between buffering and streams for a simple gzip script.

Run the buffer version:

```bash
node gzip-buffer.js
```

Run the stream version:

```bash
node gzip-stream.js
```
